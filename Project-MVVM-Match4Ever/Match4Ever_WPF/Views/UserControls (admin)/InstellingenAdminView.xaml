<UserControl x:Class="Match4Ever_WPF.Views.UserControls__admin_.InstellingenAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Match4Ever_WPF.Views.UserControls__admin_"
             xmlns:commands="clr-namespace:Match4Ever_WPF.State.Commands"
                          xmlns:comps="clr-namespace:Match4Ever_WPF.Components"
             mc:Ignorable="d" 
             Style="{StaticResource UserControlStijl}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.7*"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="1.15*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Wachtwoord veranderen" Grid.ColumnSpan="2" Style="{StaticResource InstellingenTussenTitel}"/>

            <TextBlock Text="Huidig wachtwoord" Grid.Row="1" Style="{StaticResource InstellingenTextBlock}"/>
            <comps:BindablePasswordBox Password="{Binding HuidigWachtwoord}"  Height="30" Grid.Row="1" Grid.Column="1"/>

            <TextBlock Text="Nieuw wachtwoord" Grid.Row="2" Style="{StaticResource InstellingenTextBlock}"/>
            <comps:BindablePasswordBox Password="{Binding NiewWachtwoord}" Height="30" Grid.Row="2" Grid.Column="1"/>

            <TextBlock Text="Bevestig wachtwoord" Grid.Row="3" Style="{StaticResource InstellingenTextBlock}"/>
            <comps:BindablePasswordBox Password="{Binding NiewWachtwoordBevestiging}"  Height="30" Grid.Row="3" Grid.Column="1"/>

            <Label Content="{Binding AccountInfo}" Grid.RowSpan="4" Grid.Column="2" Style="{StaticResource InstellingenInfoLabel}"/>
        </Grid>

        <Button Command="{Binding}" CommandParameter="{x:Static commands:Commands.Opslaan}" Content="OPSLAAN" Grid.Row="2" Style="{StaticResource InstellingenWijzigWachtwoordButton}"/>
        <Button Command="{Binding}" CommandParameter="{x:Static commands:Commands.Verwijder}" Content="VERWIJDER ACCOUNT" Grid.Row="2" Grid.Column="1" Style="{StaticResource InstellingenVerwijderAccountButton}"/>

    </Grid>
</UserControl>
